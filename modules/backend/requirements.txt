# NaviRaksha Backend - Render Production Dependencies
# Lightweight runtime-only (training libs excluded — models pre-loaded from .pkl)

# Web Framework
Flask==2.3.3
Flask-CORS==4.0.0
Flask-Caching==2.0.2
Flask-SQLAlchemy>=3.0.0
gunicorn==21.2.0

# Data Processing (lightweight)
pandas>=1.5.0
numpy>=1.23.0

# Database
SQLAlchemy==2.0.23

# Graph / Routing (lightweight)
networkx>=3.0

# ML inference only (no training)
scikit-learn>=1.2.0
joblib>=1.2.0

# Utilities
python-dotenv==1.0.0
requests==2.31.0
